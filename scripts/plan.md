Build geometry from geometry_spec.yaml

Mesh with refinement near interfaces

Apply thermal cycle from cycle.yaml

Add thermal strain + linear elasticity

Save fields each cycle

Extract interface features into CSV
